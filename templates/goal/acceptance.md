# 用户结果验收

> 引用单一需求决定；不能只写测试命令通过，也不把 P1 自动当成非阻塞。

| ID | 成功场景 / 约束来源 | 输入与动作 | 真实入口与观测点 | 预期用户结果 | 关键反例 / 状态组合 | 所属批次 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| A01 | | | | | | pending |

- 写入类功能覆盖实际写入到真实读回/展示；纯读或纯文档任务使用匹配其结果的观测，不虚构写 API。
- 外部供应方可替换，但应用自己的装配、解析、持久化和读取必须真实执行。
- 每项说明是本次必须完成还是已获用户确认的后续范围；未验证不能填 passed。
- 需求范围调整记录用户依据与影响，不能由 Agent 单方面 waiver 当前成功标准。
- 代码验收、真实供应方调用、目标环境检查和实际发布分别记录，不相互代替。
