# Review Kit：数据库 / Migration

## 检查项

- migration 是否可重放、可回滚或有补偿方案。
- 新字段是否有默认值、nullable 策略和历史数据处理。
- 索引是否匹配查询路径，是否可能锁表或影响线上写入。
- 事务边界是否清楚，失败是否会造成半成功。
- 字段归属是否符合资产侧/关系侧/任务侧/结果侧。
- 时间字段是否符合项目约定；PostgreSQL 优先可读时间类型。

## 高风险

- 把任务上下文字段写进资产表。
- 把可重算分析结果当成上游事实。
- 没有状态机却新增多个 status 字段。
- 只加表不说明生产者和消费者。
