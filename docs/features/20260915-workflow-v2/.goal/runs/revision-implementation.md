# 2026-09-16 定向修订实现与自测

主线程负责设计、整合与裁决；三个子 Agent 分别修改快照检查器、原型还原规则、轻量入口。详细分工与返工依据见 `../execution-log.md`。本报告是实现/自测记录，不冒充独立审查。

| 范围 | 行为与反例 | 实际检查 | 结果 |
| --- | --- | --- | --- |
| R01 / B01 | 基础范围审查后新增应拒绝；独立批次变化不误使旧证据失效；依赖返修不绕过验收 | 真实 Ruby 检查器在临时 Git 仓库的范围、契约、依赖、最终覆盖等测试 | 初轮44 tests / 138 assertions；修复验收来源后最终49 tests / 153 assertions，通过；最终整合输出见 `../validation/revision-final-structure.log` |
| R02 / B02 | 轻量落盘不升级多文档；旧实现不自动成为目标；原型输入持续传给实现/修复者 | 对具体入口与用户指南扫描，核对统一引用和局部允许差异；新增09–11场景输入 | 文本及样例结构通过；09–11 未作为真实应用行为试验运行 |
| R03 / B02 | 证据可归因且不编造用量，记录不重复膨胀 | 全仓结构检查；一个独立执行者按12号合成输入生成真实记录，再对过长表达定向修订 | 12场景结构通过；实际产物见 `../validation/revision-trials/`，不等于12次执行或真实业务效果 |
| 整合 | 固定关键词检查误把已改写规则视作丢失 | 首次仓库自检报缺界面基线；核实正文保留并加强该要求，更新原有结构检查使用的短语 | 重跑仓库检查通过；不把关键词通过当行为证据 |

原 main 干净且 HEAD 仍为 `8b59b6a2bfa41686128480fde3873cf122dee81b`；Codex/Cursor 的入口与 Goal 链接仍指向原仓库。本轮没有安装、提交、合并或调用业务服务。

已核实的改动可记 implemented；是否 accepted 等待独立审查和当前快照核对。未证明原型实际浏览器还原能力、真实 Goal token/耗时改善及交付后缺陷减少。
