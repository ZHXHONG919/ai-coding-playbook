# 本次验收

| ID | 约束 | 真实入口与观测 | 预期 |
| --- | --- | --- | --- |
| A01 | D01/D02/D05/D10 状态与覆盖 | 模板→Ruby检查器，错误状态负例 | 正确映射通过；批次范围新增/删除/修改拒绝旧证据，独立变化不误失效；并行不绕过依赖、写入范围和单片边界 |
| A02 | D01–D04/D07–D10 中文流程与提示 | 候选入口→节点规则→场景判断及执行记录产物试验 | 保留意图、原型基线和真实观测；减少无效入口负担；记录可归因且不虚构成本；交接保约束，局部隔离继续且不假完成；续跑读取交接和活动执行者、查实失联写入责任，不重开全包确认 |
| A03 | D05/D06 完整交付且隔离 | 固定候选diff、独立CR、主checkout和安装链接检查 | 规则一致、关键行为有证据、未提前应用；局限明确 |
