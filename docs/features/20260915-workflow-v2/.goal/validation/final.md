# 候选版本验证

> 历史记录：2026-09-15 的验证范围。2026-09-16 后续审查发现新问题并重新开工，当前状态见 `../status.yaml`，本轮输入见 `../cr/revision-input.md`。下文“当前”仅指当时，不代表后续候选已通过。

版本：`snapshots/final.json`，基线 `8b59b6a2bfa41686128480fde3873cf122dee81b`。内容与可执行位覆盖 102 个变更/契约文件；状态、报告及快照自身不属于被测产品代码。

## 结构与反例

`bash scripts/check-playbook.sh --repo-only` 通过；内含模板检查、25 项测试、63 个断言，0 失败。`git diff --check` 通过。修改的 Skill 入口通过 frontmatter 校验。

本轮新增检查覆盖：空/缺失契约摘要、在审内容变化、执行位变化、历史可选契约删除、当前契约缺失、批次等待环和基础批次内部等待。与原有未审完成、依赖、真实报告、最终增量等用例一起验证。

## 独立行为证据

8 个场景由未读取答案的独立 Agent 根据候选规则回答；主线程核对实际动作判断。UUID、最终业务产物、上传交互、假想旧数据、降级与真实模式、同族修复、过期状态、普通/基础任务边界均符合当前用户要求。原始回答见 `blind-replay/`；评分标准自身的路由问题及修正依据见 `first-pass.md`。

这些是场景决策回放，不是 8 次真实项目交付。回放发现的调用方矛盾已作为同族问题统一修复；不重复运行未受影响的场景来增加通过次数。

2026-09-16 更正：实际是同一独立 Agent 批量读取八例，并未遵循一例一隔离会话；准确名称为“批量盲读场景推演”。未读取答案这一事实不等于执行上下文彼此隔离。

隔离控制器试验的执行者实际修复代码，9 项测试通过；主线程另外按用户约束复核 7 组操作条件，通过。实现没有跳过补充询问，没有禁止合法手动预览，也没有误称已完成独立验收。见 `execution/execution-report.md` 与代码/测试。

## 独立 CR 与修复

首轮发现与复现见 `../cr/final-round-1.md`。修复按快照、调度、调用方三组完成；独立定向复核已通过，当前无剩余阻塞，见 `../cr/final-targeted.md`。最后两处摘要规则也由独立审查者回读确认。

独立补充验证共 31 项测试、77 个断言，包含内置的 25 项及 6 个相邻路径用例，不能相加计算覆盖。原始测试脚本归档为 `targeted-tests.rb`，保留当次执行的候选目录引用。已审脚本摘要与当前文件一致，详见独立报告。

## 隔离与限制

核实原 main 仍为 `8b59b6a` 且工作区干净；Codex/Cursor 相关安装链接仍指向原工作区。未执行安装、推送、主干合并、部署或真实供应方调用。

未证明：真实业务 Goal 的耗时/token 改善、交付后缺陷减少、浏览器或实际供应方质量。需要用户决定应用后再以真实使用记录评估；本轮不据此发布。
