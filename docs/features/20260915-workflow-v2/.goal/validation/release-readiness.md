# 发布准备度检查

2026-09-17。用户询问是否可发布；本次只做检查，正式安装、提交、合并与推送均未执行。

结论：Go，可在固定候选版本后应用到 Codex/Cursor。来源为 snapshots/parallel-final.json；本次 check-goal 与 git diff --check 通过，原 68 项/256 断言证据对应当前内容，无源码改动，未重复运行全量测试。

新增安装验证：将候选 skills/references/templates、AGENTS、安装器和两个平台配置复制到临时目录，使用 AI_CODING_SKILLS_DIR 指向临时目标。两平台各 16 个 skill 均安装成功，链接全部位于临时源，入口合成正文与候选源一致，goal-execute 指向候选副本。临时目录已自动清理。首次探针因 macOS /var 与 /private/var 的规范路径不同导致范围断言失败；统一 realpath 后通过，未据此修改产品代码。

parallel_contract_review 独立只读复核安装器、overlay、目标链接与兼容/回退，未发现阻塞；它未运行安装，实际临时试装由主线程执行。正式两个平台各 16 个已有目标仍是旧源软链，无实体目录冲突，不需 --force。原 main 干净，HEAD 为 8b59b6a2bfa41686128480fde3873cf122dee81b。

发布操作：先提交固定候选版本，保留作为软链接来源的目录，然后分别运行安装器 --target codex 与 --target cursor，核对正式入口及关联来源。无需为了安装而合并 main；若要合并或推送，另按用户授权范围执行。

回退：从保留的原源目录重新运行两个平台安装命令可恢复旧链接。软链接源必须持续保留；回退不把已创建 v2 Goal 转成旧格式，运行中的 v2 Goal 应继续引用保留的 v2 规则源或显式迁移。

剩余观察项：真实大需求中的意图偏移、UI 还原、重复验证、返工与可获得的时间/token。现有结构和局部执行证据不证明普遍效率收益；正式平台加载及真实需求执行将在应用后核实。
