# D09 全流程收口验证

本轮依据 plan.md D09 统一流程、节点职责与交接提示。原检查器、测试程序及两份机器状态模板未变；并未因文档变化重跑49项全套测试，按 autonomy-structure.log 内容摘要复用 revision-final-structure.log 中49项/153断言。旧报告只覆盖未变范围。

## 本轮新增验证

| 验证 | 实际结果 | 证据与限制 |
| --- | --- | --- |
| 协议/能力依赖结构探针 | 2项、6断言通过：协议已验收而真实能力 todo 时消费者开发可进行；真实集成越过能力依赖被拒绝 | autonomy-dependency-probe.rb / .log。临时仓库合成任务与报告仅测试状态约束，不是业务能力验收 |
| 13：局部缺口与续跑 | 主线程逐条对照6项必要行为和4项禁止行为，满足预期 | autonomy-trials/13-result.md；拆协议与能力且保留完整验收，拒绝假成功/改原型，不重新询问或全局停工；新增门店权限未知时限制受影响工作 |
| 14：最小交接与纠偏 | 主线程逐条对照5项必要行为和4项禁止行为，满足预期 | autonomy-trials/14-result.md；D1必要原文、UI采用稿实际打开、UUID/入口/范围/观测等交接保留，编译不冒充还原，拒绝擅改交互，视觉缺口局部处理 |
| 结构与格式 | 14例输入/预期结构通过，新2例路由路径存在，3个变更skill入口字段存在，Goal模板检查与 git diff --check 通过 | autonomy-structure.log；结构通过不等于14例行为通过 |
| 安装隔离 | 原 main 无未提交改动，仍为8b59b6a；Codex/Cursor入口、goal-execute、skill-maintenance链接仍解析到原仓库 | 本轮只读 git/路径工具输出；未安装、合并、推送或发布 |

13与14分别是新建、无对话历史的独立 Agent；只读各自 input 与必要候选规则，首次作答未读 expected 或其他场景。14追加读取 evidence-driven-delivery；13完成后才允许读 expected 并做定向规则复核，不能把后续复核视为另一轮盲测。原始回答由对应 Agent 原样存档，主线程负责期望对照。两项是场景决策/提示产物，不是实际业务运行、旧新对照或成本收益证据。

## 失败与修正

第一次结构探针的负例同时放了两个 in_progress，检查器先拒绝非法状态，未到预期依赖检查。修正夹具只保留当前集成任务 in_progress 后，依赖缺失按预期拒绝；保留初次输出 autonomy-dependency-probe-initial.log。没有因此修改检查器或放松状态要求。

辅助校验最初误将旧用例中的 skill 名当成路径，和普通文件 SHA 与模式摘要误比；修正检查方式，没有据错误结果改产品规则。结构信息与真实行为证据分开记录。

## 覆盖与局限

A01：复用未变检查器/状态模板证据，加本轮依赖表示探针。A02：本轮规则一致性审查、两项独立场景与交接产物；未变 D01–D08 复用 revision.md 及其索引。A03：最终主线程核对内容快照、独立复核、增量和原安装隔离。

真实项目执行、浏览器还原、真实外部存储、按时完成率、返工/token收益未在本轮验证；不能声称整体质量或效率已经提高。最终有效版本和审查裁决见 cr/autonomy-final.md 及 status.yaml 的快照指针。
