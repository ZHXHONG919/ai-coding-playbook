# 2026-09-16 候选修订验证

## 版本与覆盖

基线仍为 `8b59b6a2bfa41686128480fde3873cf122dee81b`。本轮最终范围对应 `../snapshots/revision-B01-final.json`、`revision-B02-final.json` 和 `revision-final-v2.json`；最终清单覆盖 116 个差异/契约文件。与上一阶段 `final.json` 相比，44 个文件的内容或集合发生变化，清单见 `revision-changed-files.json`。运行记录、报告、快照自身按规则排除。

| 范围 | 验证或审查依据 |
| --- | --- |
| 批次范围、相关契约、递归依赖、返修状态与通过依据 | 正式检查器测试；独立检查器审查和修复后原反例/共享恢复探针 |
| 轻量入口、原型交接与校准、中文提示、执行留证、Git 文档/检查点边界 | 独立规则审查、原反例定向复核及文件摘要核对 |
| 新增09–12场景、既有相邻eval、结构检查器调用 | 输入/预期结构检查和主线程内容核对；仅12执行了产物试验，其余新增场景不冒称行为通过 |
| 本次 Goal 的范围、验收、状态与实际结果 | 使用真实候选目录采集各批次快照并运行检查器，主线程核对证据引用与完整差异 |
| 前一阶段未变化范围 | 复用 `final.md`、`../cr/final-targeted.md` 对应的仍适用证据；本轮已发现问题及变化以新报告覆盖 |

## 实际运行结果

- `bash scripts/check-playbook.sh --repo-only`：**49 tests / 153 assertions，0失败/错误/跳过**；12组输入/预期样例结构通过。原始输出 `revision-final-structure.log`；早一版本44项输出保留在 `revision-structure.log`，不能相加。
- `git diff --check`：通过。
- 当前真实 Goal 的结构/状态检查和 B01、B02、final 快照采集：通过；不是只测试占位模板。
- 独立检查器审核最初的11项补充探针发现2个失败（模板预填通过、待复验缺少有效通过来源），修复后原11项/24断言通过；另4项/18断言覆盖完整共享验收恢复、逐项来源核对及完成门禁，通过。候选新增5项定向测试是49项的子集，不能再次累加。详见 `../cr/revision-checker.md`。
- 独立规则审核按原反例复核正常文档回写、React固定build/test、指南固定polish/audit三处修复；追加复核已授权检查点与共享验收语义。指定范围未发现剩余阻塞，详见 `../cr/revision-rules.md`。静态反例推演不算真实业务执行。

## 轻量留证产物试验

新执行者仅读取12号合成输入、执行证据规则和模板，实际生成文件；未读答案、其他场景和历史通过报告。主线程逐项核对：F1两个布局表现合为一个问题族；D2单列需求变化；修复复验与无新依据的重复CR区分；40分钟首次路径与70分钟记录窗口只据合成输入计算；模型、token和等待未知；没有把漏传原型直接认定为根因，也没有冒称真实浏览器测试。

初稿 `revision-trials/execution-evidence-initial.md` 为64行3853字符，存在重复展开。补充短任务表达规则后，同一执行者定向修订为 `revision-trials/execution-evidence-compact.md`，27行1674字符，仍保留上述事实与来源。两份都保留；后者不是第二次独立试验，字符变少也不证明token或总成本降低。

先前八例准确表述为同一独立Agent批量盲读推演；更正已写入历史验证记录及评测说明。本轮没有重跑无变化的历史控制器试验来增加通过次数。

## 安装隔离与限制

原 main 的工作区干净，HEAD仍为上述基线；Codex/Cursor的Goal与入口链接仍指向原仓库。本轮没有安装、提交、推送、合并、发布或调用业务服务。临时Git仓库的测试初始化/提交只属于检查器fixture。

已验证本轮规则一致性、具体反例修复和记录产物；尚未验证真实业务页面的浏览器还原、新策略的真实Goal耗时/token收益及交付后缺陷变化。09–11的新场景准备好但未做隔离真实执行。后续是否应用由用户决定，不能以候选验收替代真实应用效果。
