# 本次候选规则交付

范围与用户约束引用 `../plan.md` D01–D10。验证设计是否能支持中文、高效、可核验的交付；统一五节点、交接、运行时依赖与缺口处理，补原型忠实度、执行留证及并行恢复，仍不应用 Codex/Cursor、不安装、不发布。

采用 v2 continuous + functional_batch。主线程负责契约与裁决，独立Agent参与文档、验证与审查。所有未验证项如实保留；代码完成与后续实际应用分开。
