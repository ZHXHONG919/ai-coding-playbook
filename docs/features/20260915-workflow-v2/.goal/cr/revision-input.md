# 后续独立审查与用户反馈：修订输入

2026-09-16 回溯整理。此文件是主线程对已核对事实的摘要，不伪装成原始命令输出或新的独立 CR。原始调查在此前审查会话产生，关键结构反例现落实到 `scripts/test-check-goal.rb`；本轮复核见 `validation/revision.md` 与新的 CR。

| ID | 来源与观察 | 主线程裁决 / 本轮动作 |
| --- | --- | --- |
| F-SNAPSHOT-1 | 契约审查 Agent 在临时仓库复现：基础批次进入审查后新增范围内文件，旧快照仍可接受并启动依赖 | 已确认缺陷；范围内重新枚举新增、修改和删除，不能等最终检查才发现 |
| F-SNAPSHOT-2 | 同一审查的独立批次反例：B01 快照包含 B02 草稿；只改 B02 使 B01 失效；追加独立任务也使整份 slices 哈希失效 | 已确认缺陷；中间快照限定范围和相关契约，最终仍覆盖全部差异 |
| F-STATE | 上游返修后，下游已有实现无法表达待复验，只能退回 todo 或保持无效状态 | 表达缺口，不能据此断言必然重写；加入保留已有实现但不能作为依赖的待复验状态，并测恢复 |
| F-ENTRY | 流程成本审查发现轻量落盘仍触发四文件/大任务表，UI 与 Goal 入口残留全目录必读 | 已确认文本矛盾；修调用方，不增加文档数量门禁 |
| F-SOURCE | 提示审查指出旧实现事实可能被混同为有效目标；现有其他规则已有保护 | 文字歧义；明确有效规定/用户认可旧行为/观察事实，无固定新增确认环节 |
| F-UI | 用户指定的页面开发案例：实现者宣布代码完成并说明视觉未验，随后用户指出标题导出布局、筛选换行、字体、列表卡片及详情差异，要求严格遵循原型；记录曾写源码范围 Fixed、浏览器 Skipped | 已确认流程缺口；采用稿交接、首次可运行校准，视觉/交互/业务分项验收。截图与业务数据留原项目，不复制入通用规则库 |
| F-EVAL | 旧评测说明要求一例一会话，实际同一 Agent 读取八例且未读答案 | 更正为批量盲读推演；保留旧结果，不宣称隔离执行或成本改善 |

历史 `final.md` 和 `final-targeted.md` 是上一阶段证据。本轮受影响批次重开；最终是否通过以本轮固定版本验证和独立审查为准。
