# Workflow v2 修复后定向复核

结论：首轮 1–7 及本轮发现的两处同族调用方遗漏均已关闭。在本次定向复核范围内未发现剩余阻塞，可以签收修复与规则一致性；不外推为零缺陷或真实业务 Goal 验收通过。

## 复核范围

仅核对首轮 1–7 的修复、相邻合法路径，以及本次一并修改的 plan 选读表/provider 验证边界。未重做全仓专项审查；未执行安装、提交、真实供应方调用或业务仓库操作。调用方由独立子审查核对，两处补充问题已由主审查者核实；主线程修正后仅回读对应句子确认，没有重新扩大审查。

## 已关闭

| 首轮问题 | 当前结果 |
| --- | --- |
| 空清单支撑 accepted | 声明契约必须在 files 中有有效摘要；原空清单反例拒绝 |
| in_review 丢弃 changed | 已检查变化结果；覆盖内输入变化拒绝，未变化的在审输入仍可继续 |
| 批次等待死锁 | 跨批次等待环、foundation 内部 accepted 自依赖均在 Ready 拒绝 |
| 可执行位漏查 | 指纹包含可执行位；增加及撤销可执行位都会使最终快照失效 |
| 历史可选契约删除误阻塞 | 历史删除进入 changed；缺最新覆盖时报过时，FINAL 覆盖后通过；当前新采集仍拒绝不存在的契约 |
| 正式 CR 无条件降为自审 | conversation-usage 已区分轻量自审、正式独立审查和项目明确例外 |
| 旧调用方/评测仪式 | Goal 使用条件、按风险读取专项、bugfix全量/证据时机、UI证据eval、可选台账eval、8例路由已同步；最后两处回读确认见下 |

合法路径额外确认：同一普通批次内依赖 implemented 继续成立；多个无相互依赖的 foundation 任务允许同批次；已 accepted 的基础可被下一批次实际启动；普通读写权限变化不使内容/可执行位快照失效。

## 两处同族补充问题：已回读关闭

- `AGENTS.md:124` 已拆分真实 provider 与发布动作：真实调用属于本次验证且已有授权时即可执行，需明确成本与证明边界；不必等待发布。生产 SSH、发布迁移及部署仍按明确发布范围与项目 SOP。这与 provider skill 第 42–43 行一致，已授权开发验收不会被迫延期。
- `README.md:196` 已改为同一固定版本的最终视觉证据与 CR 可并行、合并到批次验收；修复后只复核受影响状态，全量按实际影响与项目要求决定。与统一证据规则和已修正 UI Drift eval 一致。

上述改动只影响规则摘要，已按主线程要求回读两句确认；脚本未改变，复用本轮已通过验证。

plan 的按问题读取表没有新增实质阻塞。复杂任务的角色责任仍可由 AGENTS 的明确条件决定，无需为此再展开一轮规则审查。

## 实际验证

- `bash scripts/check-playbook.sh --repo-only`：通过，内置 **25 tests / 63 assertions**；8 例仅结构检查，不声称本轮已重新运行 Agent 行为评测。
- `ruby /tmp/workflow-v2-targeted-tests.rb`：**31 tests / 77 assertions** 通过，包含同一套内置测试和 6 条定向补充，不能与上项相加当不同覆盖。
- `git diff --check 8b59b6a`：通过。
- 定向补充覆盖：独立新增范围不误伤在审输入；已跟踪可选契约删除的先拒绝/后覆盖；撤销可执行位；普通权限变化；独立 foundation 成员；跨批次已验收基础依赖实际启动。

两处同族文档同步已完成，未重复运行全部脚本或重启全面 CR。检查器通过仍不能证明报告真实性、审查独立性或业务验收语义，这些由实际证据与主线程裁决负责。

已审脚本 SHA-256：`check-goal.rb=5e96d3e8b9e2e1d65be1ac821fe9488b977839eb79df70698a0746c6e987a892`；`test-check-goal.rb=6962c5603686bf17442ae26ffa143cd9fada21fba52c1badcec0a16521c0d63b`。
