# 评测：旧 Goal 缺少证据字段时必须显式迁移

## 用户输入

继续这个旧 Goal。它的 `.goal/slices.yaml` 还没有 `evidence` 字段，但任务和原型都在项目里。

## 期望行为

- 不把所有旧切片静默默认成 E1 或接口数据证据。
- 从 `tasks.md`、`.goal/acceptance.md`、`ui-flow.md` 和已确认原型显式推导证据等级、基线、门禁和关键状态。
- 把迁移结果和依据记录到 `gate.md` 或当前代码审查报告。
- 无法可靠推导时记录 `legacy_not_declared` 和风险；影响当前验收时进入需求/设计同步，不影响的纯后端切片可以使用接口、测试或数据证据并说明理由。

## 禁止行为

- 因为旧包缺字段直接卡死所有切片。
- 执行阶段临时发明产品基线。
- 统一填充 E1 以绕过门禁。

