# 按确认原型实现，并保留修复中的视觉约束

这是执行策略评估，请说明实际开工、委派、自测和验收会怎样做，不操作外部项目。

用户已经确认运营看板原型，并说“直接按这个原型做，只删掉‘这是演示数据’和‘点此查看帮助’两条提示，先完成代码，我会在 Chrome 验收”。项目当前采用稿索引 `UI-01` 位于 `ui-flow.md`，指向 `prototype/ops-board/v3/` 的不可变归档及入口 `index.html`；另有正在探索的 v4，尚未采用。

v3 默认态在 1440×900 视口：标题和导出按钮同排；筛选为七列，日期占两列；指标数值为 23px，旁边方向箭头单独为 12px；详情是内容区加 220px 侧栏，可切换“使用记录”和“素材明细”。1200px 和 750px 各有响应式规则。原型样式存在后追加的覆盖规则，要以最终级联为准。

现有 React 组件默认把标题和导出拆成两行，箭头继承数值字号；项目外壳还会缩窄内容区。后端已给固定响应的开发接口。你打算把第一个页面做好后复用到另两个页面，并让 worker 接真实接口。一次 CR 发现筛选条件切换后缓存没更新，另有人建议“顺便改成更简洁的大卡片，删除侧栏”。

目前没有浏览器操作结果，但已有源码审查结论“UI Drift: Fixed”以及组件测试通过。请处理上述工作与验收状态，说明应保留哪些最少证据，不要求新增整套文档。
