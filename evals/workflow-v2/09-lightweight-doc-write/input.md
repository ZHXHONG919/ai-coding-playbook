# 保存一份轻量方案

项目已经有一个成熟的表单，所有字段、权限和保存接口都保持原样。这次只把“保存失败”的提示文案改清楚，失败时保留输入的行为也已经存在。用户说：“把刚才讨论的改法保存到 docs/features/20260916-save-message/plan.md，先不要写代码。就这一点改动，保持轻量。”

只读检查已确认当前在属于本需求的 `codex/save-message` 分支，工作区干净，项目没有额外文档模板要求，用户已授权在该分支保存方案。不需要第三方工具，也没有并行任务、模拟依赖或跨上下文恢复需求。

请说明接下来实际保存哪些内容、读取哪些规则，以及何时需要自测和审查。不要执行 Git、安装或业务代码修改。

补充上下文：当前已在本任务获授权的功能分支，存在本任务刚写的未提交改动，归属已核实；现在只是继续编辑这份轻量方案，不切分支或做 Git 合并。
